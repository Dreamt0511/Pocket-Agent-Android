---
name: bash-executor
description: Shell 命令执行器 — 在系统 shell 中执行命令并返回结果
---

## 能力描述

- **执行命令** — 在 /system/bin/sh 中执行任意 shell 命令
- **管道组合** — 支持管道、重定向等 shell 特性
- **超时控制** — 命令超时自动终止（30 秒）
- **结果解析** — 解析命令输出为结构化数据

## 使用示例

- "列出所有已安装的应用包名"
- "查看系统属性"
- "检查网络连接状态"
