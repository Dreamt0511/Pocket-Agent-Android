# Pocket-Agent 依赖文件

# 核心运行依赖
python-dotenv>=1.0.0    # 环境变量加载
nest_asyncio>=1.6.0      # 事件循环兼容
prompt_toolkit>=3.0.0    # 交互式输入框
aiohttp>=3.8.0           # 异步HTTP请求
pillow>=10.0.0           # 图像处理（截图、EXIF解析）

# 基础依赖
rich>=13.0.0    # 终端UI美化
requests>=2.25.0  # HTTP请求库（用于LLM API）

# 功能扩展依赖
geopy>=2.4.0        # GPS坐标反查地址
piexif>=1.1.3       # EXIF信息解析
psutil>=5.9.0       # 系统监控（CPU、内存、磁盘信息）

# LangChain 相关依赖
langchain>=0.2.0
langgraph>=0.2.0
langchain-openai>=0.1.0
langchain-core>=0.2.0
python-multipart>=0.0.9
packaging>=21.0               # 包版本管理（langchain 间接依赖，需显式安装）